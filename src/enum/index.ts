export const enum TimeUnit {
  'hour' = 'h',
  'minute' = 'm',
  'second' = 's',
  'a' = 'a',
}

export const enum ColumnAlign {
  left = 'left',
  center = 'center',
  right = 'right',
}

export enum LovFieldType {
  INT = 'INT',
  TEXT = 'TEXT',
  DATE = 'DATE',
  DATETIME = 'DATETIME',
  POPUP = 'POPUP',
  SELECT = 'SELECT',
}
